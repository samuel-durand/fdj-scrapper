# Configuration Nixpacks pour Railway - Backend Node.js uniquement
# Pas de build nécessaire, juste installation des dépendances

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm ci --omit=dev"]

# Pas de phase build - c'est un backend Node.js pur

[start]
cmd = "npm start"

